{
  "_comment": "Add this to ~/.claude/settings.json for Claude Code",
  "mcpServers": {
    "vaos-kernel": {
      "command": "./bin/vaos-mcp",
      "args": [],
      "env": {
        "VAOS_JWT_SECRET": "*** (min 32 bytes)"
      }
    }
  }
}
